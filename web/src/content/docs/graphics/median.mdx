---
title: Median and Rank Filters
description: A convolution cannot ignore a value. Sorting the neighbourhood gives one function that is a denoiser, an eroder and a dilator depending on the index.
section: Graphics
order: 131
---

import Playground from "../../../components/Playground.astro";

A [convolution](/learn/graphics/convolution/) is a weighted sum. Every pixel
under the kernel contributes something, and there is no weight that means
"this value is wrong, ignore it". A family of filters, the rank filters, gets
around that limit.

Sort the neighbourhood and pick by position instead.

<Playground name="09-graphics.median" />

## One function, three filters

```zig
fn rankFilter(dst: []u32, src: []const u32, comptime rank: usize) void
```

Sort the nine pixels of a 3x3 window by brightness and take the one at `rank`.
Rank 4 is the median. Rank 0 is the minimum, which is erosion. Rank 8 is the
maximum, which is dilation. Basic morphology and the standard denoiser are
all the same loop with a different index. Because the choice is comptime,
each one compiles to its own specialised function instead of a branch in the
inner loop.

The output checks the morphology at the end: the block covers 153
pixels brighter than 200, which erosion cuts to 105 and dilation grows to 209.
Bright regions shrink and grow by a pixel of boundary. Running one then the
other is opening and closing, which is how small specks get removed without
changing the size of anything large.

## Why the median beats the blur on this noise

Salt and pepper noise replaces a pixel completely with black or white. The
corrupted value has no relationship to what was there. A weighted average
handles this case badly:

```
            black white  mean luma error vs clean
     noisy:    54    75        9.03
  gaussian:     0     0       11.80
    median:     0     2        1.59
```

If you read only the first two columns, the gaussian looks perfect: not a
single pure black or pure white pixel survives. But those two columns do not
measure image quality. The gaussian removed each noisy pixel by averaging it
into a grey smudge nine pixels wide. The third column shows the cost: mean
error against the clean picture went *up*, from 9.03 to 11.80. After the
gaussian, the image is further from the clean picture than the noisy input
was.

The median throws the outlier away. In a window of nine where one value is
extreme, the extreme value sorts to an end, and the middle element is one of
the eight that were fine. Error drops to 1.59.

This holds in general. Even a few outliers pull a mean away from the true
value. A median ignores outliers completely, as long as they are fewer than
half of the values. For the same reason, statistics reports medians for
income.

## And why it keeps the edge

```
     x    |    5    6    7    8    9   10   11   12
 original |   24   24   25  212  212  212  212  212
 gaussian |   23   24   71  165  212  212  212  212
   median |   24   24   25  212  212  212  212  212
```

The gaussian softens the block's edge, because at a pixel on the boundary the
window straddles both sides and the average lands in between. The median does
not move it at all. At any pixel on the dark side, at least five of the nine
neighbours are still dark, so the middle element is dark. The edge stays
exactly where it was.

Rank filters are used because they smooth without moving edges. They cost
more than a blur: nine values sorted per pixel, against nine multiplies and
one divide.

## The sort

Nine elements, so insertion sort:

```zig
var i: usize = 1;
while (i < window.len) : (i += 1) {
    const key = window[i];
    var j = i;
    while (j > 0 and canvas.luma(window[j - 1]) > canvas.luma(key)) : (j -= 1) {
        window[j] = window[j - 1];
    }
    window[j] = key;
}
```

The quadratic bound does not matter at this size, and insertion sort has
almost no constant overhead. Production implementations go further and use a
fixed compare-exchange network, in straight-line code with no loop and no
data-dependent branches at all. A mispredicted branch per pixel is expensive,
and a network has no branches. A network can also be vectorised directly,
which the loop above cannot.

The size of the network depends on what you ask for. Sorting nine elements
takes 25 compare-exchanges, but the median alone takes 19. A selection
network only has to put the middle value in place, and may leave the rest of
the window in any order.

## Sort whole pixels, not channels

The window holds packed pixels and sorts them by luma, so the output is a
colour that actually appeared in the neighbourhood. Taking the median of the
red channel, the green channel and the blue channel independently is easier,
and it is wrong. The three medians can come from three different pixels,
producing a colour that was nowhere in the window. On coloured noise, this
creates colours that were never in the image.

## Where the family goes next

The median's weakness is that it treats every pixel in the window as equally
relevant regardless of how far away it is or how different it is. The
bilateral filter fixes that by weighting each neighbour by both distance and
colour difference, which smooths flat regions while leaving edges alone even
better than a median does. It is a weighted average again, but with weights
that depend on the data, so it is not a convolution either.
