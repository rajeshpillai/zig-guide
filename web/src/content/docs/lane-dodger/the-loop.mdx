---
title: The Loop Takes No dt
description: A fixed timestep, an accumulator, interpolation for the leftover, and the split that lets a browser own the loop.
section: Lane Dodger
order: 30
---

import GameSource from "../../../components/GameSource.astro";

The obvious game loop reads the clock and hands the elapsed time to everything
else.

```zig
while (running) {
    const dt = getFrameTime();
    world.update(dt);   // do not do this
    draw(world);
}
```

It costs three things.

**The same inputs stop producing the same run.** `dt` is whatever the machine
delivered. A 60 Hz laptop and a 165 Hz monitor step the world differently.
Adding 1/60 sixty times does not give the same number as adding 1/165 a hundred
and sixty five times.

**A slow frame becomes an advantage.** This game scores survival. A frame that
takes 50 ms advances the world 50 ms in one jump, and collision is tested at the
ends of that jump rather than through it. Stutter and you pass through a block.

**Nothing replays.** A crash report becomes a video instead of a seed and a list
of keypresses.

## Fixed steps

`step` takes no `dt`. It advances one tick, always the same size.

<GameSource file="src/sim/sim.zig" decl="step" />

There is no way to call it wrong. A caller who wants half a tick cannot get one.
A caller who wants ten calls it ten times.

The tick rate is 120 Hz, above every common refresh rate.

## The accumulator

Real time arrives in uneven lumps. The loop banks it and spends it one tick at a
time.

<GameSource file="src/main.zig" decl="advance" />

Input goes to the first tick of the frame and is empty for the rest. Presses are
edge triggered, so handing one press to three ticks would cross three lanes on a
single tap.

A frame is clamped before it reaches the accumulator.

<GameSource file="src/main.zig" decl="max_frame_time" />

Without that clamp, a paused game or a laptop waking from sleep hands the loop
ten seconds. That becomes 1200 ticks, which takes long enough to produce another
large `dt`. The game freezes rather than skips.

## The leftover

After the tick loop there is always less than one tick of time left over.
Ignoring it makes the game step visibly at 120 Hz on a 165 Hz display.

So the renderer is told how far into the next tick it is, and draws between the
last two states.

```zig
const alpha = self.accumulator / config.tick_dt;
```

<GameSource file="src/render/draw.zig" decl="lerp" />

This is why `Entity` carries a `y_prev`. No rule reads it. It exists so the
renderer can draw a frame that falls between two ticks.

## One frame, not a loop

On the desktop the loop is a `while`. In a browser it cannot be. Emscripten
calls a function once per animation frame and never returns into your code, so
there is nowhere for a `while` loop to live and nothing for its locals to live
in.

So "one frame" and "keep doing frames" are separate. `Game` is a struct with a
`frame` method, and only the caller differs.

<GameSource file="src/main.zig" decl="main" />

The game state is a file scope variable rather than a local.
`emscripten_set_main_loop` never returns, so a local in `main` would be gone by
the first frame.

The split is worth making even for a game that will never ship to a browser. A
frame you can call is a frame you can call six hundred times with a bot at the
controls and no window open, which is what
[the difficulty tests](/learn/lane-dodger/fair-by-design/) do.
